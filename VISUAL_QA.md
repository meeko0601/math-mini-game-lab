# VISUAL QA

2026-09-22にWebDevプレビューで確認。

| 画面 | 1280×800 | 375×812 | 結果 |
|---|---:|---:|---|
| ホーム | 確認済み | 確認済み | 3カード、タイトル、音ボタンが読みやすく、重なりなし |
| 数える | 確認済み | 確認済み | 問題、対象、4つの回答が1画面で操作可能 |
| 運ぶ | 確認済み | 確認済み | PCは左右、スマートフォンは上下配置になり、対象と運び先が明確 |
| 選ぶ | 確認済み | 確認済み | 6つの大きな選択領域と目標図形が1画面に収まる |

上記4画面はiPad縦向き相当の768×1024でも追加確認済み。ホームは3列、各ゲームは広いタップ領域を保ったまま1画面内に収まり、重なりや切れはなかった。

ホーム画面は淡い中立色、各ゲームは設定由来の仮テーマ色を使っている。外部画像はなく、絵文字とCSS図形のみ。PCとスマートフォンのどちらでもホームボタンと音ボタンが常に見える。次に実操作の正解・再挑戦・次問題・ホーム復帰を確認する。

公開プレビューの初回確認でViteのホスト許可エラーを検出したため、`vite.config.js` に `server.allowedHosts: true` を追加して再起動した。WebDevプレビューのホーム再描画は正常。

修正後の公開URLは実ブラウザで正常に開いた。ホームの3カードと音ボタンが操作要素として認識され、数えるカードを押すと `#game=count` へ遷移し、ランダム個数と4つの回答ボタンが表示された。

数えるゲームで誤答を押すと「だいじょうぶ。もういちど みてみよう」が表示され、回答は続行できた。続けて正答を押すとボタンが緑になり、短い記号の紙吹雪、肯定的な言葉、「つぎへ」ボタンが表示された。

「つぎへ」を押すと対象、個数、選択肢が新しい問題へ更新された。「おうち」を押すとハッシュが解除され、3カードのホーム画面へ戻った。

運ぶゲームをホームから開始できた。表示された「2こ」の問題で、ドラッグ代替の通常タップを1回行うと、対象が1個減り、運び先の表示が `0 / 2` から `1 / 2` へ更新された。

同じ問題でもう1個をタップすると `2 / 2` になり、運び先が緑へ変化し、肯定的な言葉と「つぎへ」が表示された。正解後のホーム復帰も正常。

選ぶゲームをホームから開始できた。目標図形と必要個数、6つの大きな選択ボタンが表示された。誤った図形を押すと「おしい！ ほかも みてみよう」が表示され、問題は続行できた。

正しい図形を1個押すと選択済みの薄い表示になり、同じボタンの重複回答を防ぐ状態になった。2個目の正しい図形を押すと正解演出、肯定的な言葉、「つぎへ」が表示された。

Pointer Eventsのドラッグ経路を座標付きイベントで検証したところ、移動中の物自身が `elementFromPoint` の結果になり、運び先判定を隠す問題を検出した。タップ代替は正常だが実ドラッグに影響するため、運び先の矩形とリリース座標を直接比較する方式へ修正する。

座標ベースのドロップ判定へ修正後、タッチ型Pointer Eventsで物を運び先へ移動するとカウントが `0` から `1` へ増え、対象が1個減ることを確認した。運び先の外へ離した場合はカウントと対象数が変わらず、「はこの なかまで はこんでみよう」が表示された。

ゲーム中にスピーカーボタンを押すと `🔊` / `🔇` とアクセシブル名「おとを けす」/「おとを だす」が切り替わり、もう一度押すと戻った。

実ブラウザがドラッグ後に生成する追加の `click` を想定して抑止処理を追加した。運び先の外では追加クリック後もカウント0・対象数不変、運び先の内側ではカウント1・対象数が1個だけ減り、二重加算されないことを確認した。

## 透過PNG組み込み（2026-09-23）

- 付属の軽量・透過PNG 15枚を `client/public/assets/` に配置し、設定ファイルの素材一覧へ登録。
- PC幅でホームのチーム画像、3カードの小物画像、3ゲーム共通のルーペ、正解時のはなまるを確認。画像の欠け、比率崩れ、重なりなし。
- 「かぞえよう」の正解操作、「はこんでみよう」「みつけよう」の初期表示が従来どおり動作することを確認。
- 375×812でホームを確認。チーム画像は332×187px、カード画像は58×58px、横方向のはみ出しなし。
- 375×812のゲーム画面で問題横画像は46×46px、数字ボタンは高さ70px、縦横とも主要操作が1画面内に収まる。
- キャラクター候補・マスコット候補は画面へ割り当てず、設定内の候補一覧として保持。
